import type { NextPage } from 'next';
import Head from 'next/head';
import Image from 'next/image';

import hero from '../assets/images/background.png';

const Home: NextPage = () => {
  return (
    <div>
      <Head>
        <title>Create Next App</title>
        <meta name="description" content="Generated by create next app" />
      </Head>

      <main className="flex items-center justify-center w-screen h-screen ">
        <div className="fixed h-screen w-screen overflow-hidden -z-10 ">
          <Image
            alt="Hero"
            src={hero}
            layout="fill"
            objectFit="cover"
            quality={80}
            priority={true}
          />
        </div>

        <div className="grid grid-cols-8 grid-rows-3 gap-y-4">
          <div className="col-span-2 row-span-1 bg-red-900 py-5">
            <p className="-rotate-90">Signin</p>
          </div>
          <div className="col-span-6 row-span-3 bg-blue-900">big box</div>
          <div className="col-span-2 row-span-1 bg-green-900 py-5">
            <p className="-rotate-90">Register</p>
          </div>
        </div>
      </main>
    </div>
  );
};

export default Home;
